(function () {
    Paginator.ComponentSettings = function Settings(opts) {
        var settings;

        settings = {
            padding: opts.padding,
            paperGap: opts.paperGap
        };

        return settings;
    };
})();
